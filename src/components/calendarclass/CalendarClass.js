import React from 'react';
import SectionTitle from '../sectiontitle/SectionTitle';

const CalendarClass = () => {
  return <SectionTitle title="CALENDÁRIO DE AULAS" />;
};

export default CalendarClass;
