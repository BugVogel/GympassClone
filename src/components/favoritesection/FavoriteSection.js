import React from 'react';
import SectionTitle from '../sectiontitle/SectionTitle';

const FavoriteSection = props => {
  return <SectionTitle title="Favoritos" />;
};

export default FavoriteSection;
